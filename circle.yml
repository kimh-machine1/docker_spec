machine:

  services:
    - docker

#dependencies:
#    override:
#        - docker build .
dependencies:
  post:
    - sudo date -s "$(curl -sD - google.com | grep '^Date:' | cut -d' ' -f3-6)Z"

test:
  pre:
    - echo "thisis ${hoge}  some space"

  override:
    - ruby -v

deployment:
  master:
    branch: master
    commands:
      - echo "$CIRCLE_USERNAME"
