machine:

  services:
    - docker

#dependencies:
#    override:
#        - docker build .

test:
  pre:
    - echo this is pre
    
  override:
    - ruby -v

deployment:
  master:
    branch: master
    commands:
      - echo "$CIRCLE_USERNAME"
