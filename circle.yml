machine:

  services:
    - docker
    - cassandra

#dependencies:
#    override:
#        - docker build .

test:
  override:
    - ruby -v

deployment:
  master:
    branch: master
    commands:
      - echo "$CIRCLE_USERNAME"
