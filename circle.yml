dependencies:
  pre:
    - sudo add-apt-repository ppa:beineri/opt-qt541-trusty -y
    - sudo apt-get update
    - sudo apt-get install -y qt54quickcontrols qt54webengine
    - source /opt/qt54/bin/qt54-env.sh && bundle install

test:
  override:
    - ruby -v
