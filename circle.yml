machine:

  services:
    - docker
    - mongodb

#dependencies:
#    override:
#        - docker build .


test:
  pre:
    - exit 100

  override:
    - ruby -v

deployment:
  master:
    branch: master
    commands:
      - echo "$CIRCLE_USERNAME"
