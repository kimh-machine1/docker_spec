machine:
  python:
    version: 3.4.2

# dependencies:
#   pre:
#     - sudo service docker stop; sudo curl -L -o /usr/bin/docker 'http://s3-external-1.amazonaws.com/circle-downloads/docker-1.6.0-circleci'; sudo chmod 0755 /usr/bin/docker; sudo service docker start; tr

test:
  override:
    - ruby -v

# deployment:
#   hub:
#     commands:
#       - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#       - docker push $DOCKER_REPO:$CIRCLE_BUILD_NUM
deployment:
  production:
    codedeploy:
      foo:
        deployment_group: foo
        deployment_config: foo
