machine:
  services:
    - docker

database:
  override:
    - echo aaa

test:
  override:
    - echo "aaa"


notify:
  webhooks:
    # A list of hashes representing hooks. Only the url field is supported.
    - url: $foo
