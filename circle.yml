machine:
  ruby:
    version: 2.2.0

  services:
    - docker
    - cassandra

#dependencies:
#    override:
#        - docker build .
dependencies:
  override:
    - bundle install --retry 3

test:
  override:
    - ruby docker_spec.rb

deployment:
  master:
    branch: master
    commands:
      - echo "$CIRCLE_USERNAME"
