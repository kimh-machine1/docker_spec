machine:
  ruby:
    version: 2.2.2
    
database:
  override:
    - echo aaa

test:
  override:
    - curl -X POST --data-urlencode "payload={'channel':'#critical_information','username':'CircleCi'}" https://hooks.slack.com/services/foobar

