machine:
  environment:
    PACKER_VERSION: 0.7.5

  services:
    - docker

dependencies:
  pre:
    - bash ./cisetup

test:
  pre:
    - exit 100

  override:
    - ruby -v

deployment:
  master:
    branch: master
    commands:
      - echo "$CIRCLE_USERNAME"
