machine:
  ruby:
    version: 2.1.5

  services:
    - docker

#dependencies:
#    override:
#        - docker build .
dependencies:
  override:
    - bundle install:
        timeout: 180
        retry: 20

test:
  override:
    - ruby docker_spec.rb

deployment:
  master:
    branch: master
    commands:
      - echo "$CIRCLE_USERNAME"
