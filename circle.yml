dependencies:
  pre:
    - go get -u git@github.com:BrightLocal/ipv6client


machine:
  services:
    - docker

database:
  override:
    - echo aaa

test:
  override:
    - echo "aaa"


notify:
  webhooks:
    # A list of hashes representing hooks. Only the url field is supported.
    - url: https://someurl.com/hooks/circle