machine:
  python:
    version: 2.6.6
    
test:
  override:
    - ruby -v



# deployment:
#   hub:
#     commands:
#       - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#       - docker push $DOCKER_REPO:$CIRCLE_BUILD_NUM
#
#
#
#
